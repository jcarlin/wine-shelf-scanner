# Google Cloud Vision API
GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account.json

# Environment
ENVIRONMENT=development

# LLM for OCR normalization (Phase 3)
# Choose provider: "claude" or "gemini" (default: claude)
LLM_PROVIDER=claude

# API key for your chosen provider (only one needed)
# Get Claude key from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=your-claude-api-key-here
# Get Gemini key from: https://aistudio.google.com/apikey
# GOOGLE_API_KEY=your-gemini-api-key-here

# Mock mode (set to "true" to use mock responses)
USE_MOCKS=true
